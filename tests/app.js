writeFile("fromjs.txt", "bonjour")
// demo()


for (let i = 0; i < 4; i++) {
  // task(0)
  // sayHi("Rucola")
  console2.log("index" + i)
  console2.log(" ----- in app.js: (" + i + ") " + multiply(2, i + 0))
  // demo()
}
